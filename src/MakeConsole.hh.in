#ifndef MAKECONF_H_
#define MAKECONF_H_

// Include the console files
#include "${CMAKE_SOURCE_DIR}/${Adventure_CONSOLE_FILE}"

// Typedef the selected console as "DefaultConsole"
namespace Interfaces{
    typedef ${Adventure_CONSOLE_CLASS} DefaultConsole;
}

#endif // MAKECONF_H_
